"""
integrations/ — подключения к внешнему миру.

Каждая интеграция — отдельный файл с единым интерфейсом BaseIntegration.
Подключить новую: создать файл, зарегистрировать в core.py.

Текущие:
  (пока пусто — каркас готов)

Запланированные:
  weather.py     — погода (OpenWeatherMap / OpenMeteo)
  calendar.py    — Google Calendar
  smart_home.py  — Home Assistant / MQTT
  sensors.py     — физические датчики (температура, движение)
  notifications.py — push через FCM/APNs
"""
